# Intent Anaylsis Model

This is the model that determines wether or not something is safe or worth searching the internet to find the best information.

```bash
ollama create intent_analysis -f ./Modelfile
```
