# Search Query Generation Model

This is the model that generates search queries when deemed necessary. Trained to generate meaningful queries based on the prompt.

```bash
ollama create search_query_generation -f ./Modelfile
```
