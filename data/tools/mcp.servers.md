# mcp.servers

## Description

List configured MCP servers visible to the current user, including global servers and the current user's own servers. Use this when external MCP-backed capabilities may be needed and the visible builtin tools are insufficient. Server URLs and credentials are never returned.

## Parameters

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
